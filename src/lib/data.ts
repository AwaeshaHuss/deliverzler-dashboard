// This file is no longer needed for mock data as the app is connected to Firebase.
// You can use it for seeding your database or for local testing.

import type { User, Driver, Order, MenuItem, Promotion, Review } from './types';

export const users: User[] = [];
export const drivers: Driver[] = [];
export const orders: Order[] = [];
export const menuItems: MenuItem[] = [];
export const promotions: Promotion[] = [];
export const reviews: Review[] = [];
